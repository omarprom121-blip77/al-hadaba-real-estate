import {NextResponse} from 'next/server';import {cookies} from 'next/headers';import {verify} from '../../../../lib/auth';import db from '../../../../lib/db';import {ObjectId} from 'mongodb';
async function guard(){const c=await cookies();return verify(c.get('admin_token')?.value)?.role==='admin'}
export async function GET(){if(!(await guard()))return NextResponse.json({error:'غير مصرح'},{status:401});const client=await db;const items=await client.db(process.env.MONGODB_DB).collection('comments').find({}).sort({createdAt:-1}).toArray();return NextResponse.json({items:items.map(x=>({...x,_id:x._id.toString()}))})}
export async function PATCH(req){if(!(await guard()))return NextResponse.json({error:'غير مصرح'},{status:401});const {id,status}=await req.json();if(!['approved','pending','hidden','rejected'].includes(status))return NextResponse.json({error:'حالة غير صحيحة'},{status:400});const client=await db;await client.db(process.env.MONGODB_DB).collection('comments').updateOne({_id:new ObjectId(id)},{$set:{status}});return NextResponse.json({ok:true})}
export async function DELETE(req){if(!(await guard()))return NextResponse.json({error:'غير مصرح'},{status:401});const id=new URL(req.url).searchParams.get('id');const client=await db;await client.db(process.env.MONGODB_DB).collection('comments').deleteOne({_id:new ObjectId(id)});return NextResponse.json({ok:true})}