import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function LoginClient(){const [phone,setPhone]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const router=useRouter();
async function go(e){e.preventDefault();setError('');const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});const d=await r.json();if(!r.ok)return setError(d.error||'بيانات الدخول غير صحيحة');router.push('/admin');}
return <main className="auth"><form className="form auth-box" onSubmit={go}><div className="eyebrow dark">لوحة الإدارة</div><h1>تسجيل الدخول</h1><input required placeholder="رقم الهاتف" value={phone} onChange={e=>setPhone(e.target.value)}/><input required type="password" placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn primary">دخول</button>{error&&<small className="error">{error}</small>}<a href="/" className="text-link">العودة للموقع</a></form></main>}